"use client";

import StudentHome from "@/components/StudentHome";

/** Minha Jornada — integrante da equipe (Q1: o login é próprio, a liderança é de um só). */
export default function IntegranteDashboard() {
  return <StudentHome tasksHref="/integrante/tarefas" />;
}
