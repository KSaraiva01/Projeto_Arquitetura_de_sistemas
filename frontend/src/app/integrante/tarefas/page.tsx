"use client";

import StudentTasks from "@/components/StudentTasks";

/** Minhas Tarefas — integrante: vê as tarefas da equipe e também entrega (perfil com acesso próprio). */
export default function IntegranteTarefasPage() {
  return <StudentTasks />;
}
